{
  "private": true,
  "dependencies": {
    "fs": "0.0.1-security",
    "readline": "^1.3.0",
    "test": "^0.6.0"
  },
  "devDependencies": {
    "ava": "*",
    "eslint": "^6.8.0",
    "eslint-config-google": "^0.14.0",
    "nyc": "*"
  },
  "scripts": {
    "pretest": "npm run eslint",
    "test": "ava --watch",
    "coverage": "nyc ava",
    "eslint": "eslint --ext .js --ignore-pattern .gitignore ."
  }
}
